# Root requirements (mirrors api/requirements.txt, which is the one Vercel's
# @vercel/python builder actually installs — it looks next to the functions).
# The bentlyk core is dependency-free; only the Postgres driver is needed.
psycopg[binary]>=3.1
